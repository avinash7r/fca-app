services:
  server:
    image: localhost:5000/fca-server
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "7777:7777"
    env_file:
      - ./server/.env

  client:
    image: localhost:5000/fca-client
    build:
      context: ./client
      dockerfile: Dockerfile.prod
    ports:
      - "5173:80"
    depends_on:
      - server
